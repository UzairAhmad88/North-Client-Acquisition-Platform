# Enterprise Knowledge Graph
Graph database model linking Entities (Customer, Project, Requirement, Code, Service, Data, Model, Decision) via typed relationships (OWNS, USES, DEPENDS_ON, PRODUCES, CONSUMES).
Supports multi-hop traversal queries.
