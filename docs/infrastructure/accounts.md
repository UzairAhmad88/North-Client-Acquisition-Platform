# Cloud Accounts & IAM Governance
Maintains the centralized inventory of cloud tenant accounts. Zero plaintext credentials are ever returned via REST APIs. Credentials are stored securely via the Phase 66 encrypted secrets vault.
