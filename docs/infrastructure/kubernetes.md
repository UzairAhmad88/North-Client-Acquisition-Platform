# Kubernetes Operating Platform
First-class operating layer across multi-cloud Kubernetes clusters. Collects real-time events, pod restart loops, scheduling failures, and admission controller violations.
