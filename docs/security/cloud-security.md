# Cloud Security Posture Management (CSPM)

## 1. Cloud Misconfiguration Detection
Continuously scans multi-cloud environments for:
- Publicly accessible S3/Blob storage buckets.
- Wildcard IAM permissions (`*:*`).
- Unencrypted storage volumes and databases.
- Open security groups (0.0.0.0/0 on sensitive ports).
