import React from 'react';
import { DataCommandCenterDashboard } from '@/components/data_os';

export const metadata = {
  title: 'Autonomous Data & Knowledge OS | Uzaii',
  description: 'Enterprise Data Command Center, Connectors, Pipelines, Lakehouse, Knowledge Graph & Governance',
};

export default function DataPlatformPage() {
  return <DataCommandCenterDashboard />;
}
